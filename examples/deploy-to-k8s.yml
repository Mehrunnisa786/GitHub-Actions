name: Deploy to k8s

on:
  push:
    branches:
      - main

env:
  KUBECONFIG: ${{ secrets.KUBECONFIG }}

jobs:
  deploy:
    runs-on: kubernetes
    steps:
    - name: Checkout code
      uses: actions/checkout@v2   #plugins

    - name: Create Docker Image
      uses: docker/build-push-action@v2     #plugins
      with:
        context: .
        push: true

    - name: Deploy to k8s
      uses: appleboy/kubectl-action@v1.0.0  #plugins
      with:
        args: apply -f deployment.yaml
        env:
          KUBECONFIG: ${{ env.KUBECONFIG }}
